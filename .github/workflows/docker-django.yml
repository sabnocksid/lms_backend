jobs:
  deploy:
    runs-on: self-hosted 
    steps:
      - uses: actions/checkout@v3

      - name: Run docker-compose locally
        run: |
          cd /path/to/your/lms-backend
          docker-compose down
          docker-compose up -d --build